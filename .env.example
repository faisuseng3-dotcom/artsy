DATABASE_URL="postgresql://user:password@localhost:5432/artsy?schema=public"

NEXTAUTH_SECRET="change-me"
NEXTAUTH_URL="http://localhost:3000"

# Optional — AI listing assistant falls back to a rules-based draft without it
OPENAI_API_KEY=""

# Optional — checkout shows a clear "payments not configured" state without it
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Optional — image uploads fall back to local disk storage in dev without it
S3_BUCKET=""
S3_REGION=""
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""
S3_ENDPOINT=""
